//Create , Input, Output, Update -- Array Operations..
// 0-based indexing -- C++, python, java 
//Doesn't support multiple datatypes in same array. (Unlike python) --> There is specified datatyp of array.
//Remember -- Array's size is static . Once declared, it can't be changed.

//Array - List of elements of the same datatype in a contigous memory location.
public class array {
    public static void main(String[] args) {
        //creating an array
        //dataype array_name = new dataType[size]
        // (declare part)    = (creating part)
        int marks[]= new int[50];
        //or
        int numbers[]= {1,2,3};   //-- if we enter elements itself, it isn't neccessary to specify size 
        String fruits[]= {"Apple","Mango","Orange"};
        

    }
}
