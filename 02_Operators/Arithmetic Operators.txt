@Binary Operators --> +,-,*,/,%

@Unary Operators --> ++ (increment), --(decrement)
// a=a+1 -- a++(post increment --> value use, value change) or ++a(pre increment --> value change,value use) 
// a=a-1 -- a--(post discrement) or --a(pre discrement)
